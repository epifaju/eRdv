# Configuration de la base de données H2 (pour développement local)
spring.datasource.url=jdbc:h2:mem:erdv_db
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# Configuration JPA/Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# Console H2 (accessible sur http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Configuration JWT
jwt.secret=your-secret-key-here-make-it-very-long-and-secure-for-production
jwt.expiration=86400000

# Configuration SMTP (Gmail example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuration du serveur
server.port=8080
server.servlet.context-path=/api

# Configuration CORS (gérée par CorsConfig.java)
# Les paramètres CORS sont maintenant configurés programmatiquement

# Logging
logging.level.com.erdv=DEBUG
logging.level.org.springframework.security=DEBUG

# Allow circular references (temporary fix for dependency cycle)
spring.main.allow-circular-references=true

# Jackson configuration is now handled by JacksonConfig.java 